# Mise.ZSH 
#
# Using Chezmoi templating, caches the evaluation of Mise's activate command for performance.
{{- if lookPath "kubectl" }}
{{ output "zsh" "-c" "kubectl completion zsh" | trim }}
{{- end }}
